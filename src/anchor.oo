// # src/ — Not the homepage
//
// Logline: Static index.html is the live home. No hash router.
//
// Setup: GitHub Pages, no build. The old theme/route/sky SPA is gone
//        so it cannot overwrite the home body. Do not invent a new SPA.
//
// Beats:
//   1. Public home is index.html (install box + constellation, including cli).
//   2. 404.html is static. There is no data-page router.
//   3. Do not fetch /docs/*.oot into #content. That path is dead.
