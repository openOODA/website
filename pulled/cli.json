{
  "repo": "cli",
  "title": "openOODA CLI",
  "source": "openOODA/cli/docs/",
  "content": "## ooda — Workflow Driver\n\n`ooda` is the command-line workflow driver. It dispatches to the compiler (`oodac`), the package manager (`opm`), the language server (`lsp`), the MCP server (`mcp`), and runs scripts via the toolchain.\n\n### Install\n\n```sh\ncurl -fsSL https://openooda.org/install.sh | bash\n```\n\n### 13 Subcommands\n\nThe actual `cli/main.oo` source dispatches the following:\n\n| Command | Purpose |\n|---------|---------|\n| `ooda build` | Build a `.oo` source file to a target (ELF, WASM, C99, LLVM IR) |\n| `ooda run` | Compile and execute a `.oo` source file |\n| `ooda gen` | Generate code (AST → target) without executing |\n| `ooda test` | Run tests (with sequential double-run invariant) |\n| `ooda install` | Install the toolchain or a package |\n| `ooda bench` | Run benchmarks |\n| `ooda init` | Initialize a new project |\n| `ooda context` | Inspect context (LLM-facing: token budget, caps, audit) |\n| `ooda health` | Server health check |\n| `ooda fix` | Auto-repair a `.oo` source file (adversarial repair) |\n| `ooda digest` | Compute the Merkle digest of a source tree |\n| `ooda fmt` | Format a `.oo` source file |\n| `ooda token` | 12 token sub-subcommands (issue, attenuate, verify, etc.) |\n\nPlus spawned subcommands: `opm`, `lsp`, `mcp` (delegated to those repos).\n\n### Environment variables\n\n| Variable | Purpose |\n|----------|---------|\n| `OODA_REPO_ROOT` | Root of the openOODA workspace |\n| `OODA_FS_WRITEDIR` | Allowed write directory (sandboxed) |\n| `OODA_CACHE` | Cache directory for incremental builds |\n| `OODA_TOOLCHAIN` | Override the toolchain path |\n| `OODA_PROFILE` | Profile name (debug, release, pgo) |\n\n### Flags\n\n- `-h`, `--help` — show help\n- `-v`, `--version` — show version\n- `--strict` — fail on any warning\n- `-O<0|1|2|3>` — optimization level\n- `--target=<elf|wasm|c99|llvm>` — emission target\n- `--pgo-generate` / `--pgo-use` — profile-guided optimization\n\nFull docs: [github.com/openOODA/cli/tree/main/docs](https://github.com/openOODA/cli/tree/main/docs)"
}
