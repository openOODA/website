# TRUTH DRIFT — Stale versions, deleted boards, wrong counts
File: website/audit/truth-drift.oot

Verdict: PASS on the public copy after the 2026-09-04 repair.
Previous FAIL (deleted ALLCAPS boards, JSON pulls, `cli` card,
stale tags) is closed.

## 1. llms.txt

Public LLM manual at `/llms.txt`. Read order matches
`openOODA/ANCHOR.oo`. The 13-board ALLCAPS set is named only
as deleted. 12-repo table uses substratum order and tags
dated 2026-09-04.

## 2. pulled/*.oot vs git tags (2026-09-04)

| Route | Tagline | Disk tag |
|-------|---------|----------|
| openOODA | v1.0.20, 12 repos | v1.0.20 |
| oodar | v4.0.0 | v4.0.0 |
| oodac | v0.2.2, 12 subdirs | v0.2.2 |
| std | v1.0.8, 188 ANCHOR.oo | v1.0.8 |
| ooda | v0.2.7, 13 commands | v0.2.7 |
| packaging | v0.1.6 | v0.1.6 |
| opm | v0.8.8 | v0.8.8 |
| catalog | v0.0.1 | v0.0.1 |
| lsp | v0.6.12 | v0.6.12 |
| mcp | v0.6.10, 22 tools | v0.6.10 |

website and .github stay in the home table. They are not nav
routes. Cache query is `?v=20260904j`.
Each pulled `Source:` line is `openOODA/<repo>/docs/`.
That directory has `docs/ANCHOR.oo` in the cited repo.

## 3. License

`LICENSE` is MIT. `README.md` says MIT. The dual-license
sentence is gone.

## 4. Sitemap and guides

`sitemap.xml` lists `/` and `/llms.txt` with lastmod
2026-09-04. Hash routes are not sitemap URLs. That is the
SPA residual. `website/guide/` still does not exist.
WORKFLOW.oot §0 names that path. Do not recreate it here.

## 5. Next repair

Refresh pulled tags when a product repo ships a new tag.
Keep `index.html` home body in lockstep with
`pulled/openOODA.oot`.
