# TRUTH DRIFT — Stale versions, deleted boards, wrong counts
File: website/audit/truth-drift.oot

Verdict: PASS for the public home path after 2026-09-15.
Live home is static `index.html`. Constellation includes `cli`.
`pulled/` is gone; do not resurrect hash-route snapshots.

## 1. llms.txt

Public LLM manual at `/llms.txt`. Read order matches
`openOODA/anchor.oo`. The ALLCAPS board set is named only
as deleted. Polyrepo table includes `cli`. Home is static
`index.html`, not `.github/docs/HOME.oot`.

## 2. Home constellation (2026-09-15)

Strategic: openOODA.
Operational: ooda, cli, tui, install, opm, catalog, lsp, mcp, bb.
Tactical: oodac, oodar, std.

website and .github are table-only. Home lists 13 product
repos. There is no `pulled/` cache and no `?v=` bust.

## 3. License

`LICENSE` is MIT. `README.md` says MIT.

## 4. Sitemap

`sitemap.xml` lists `/` and `/llms.txt` with lastmod
2026-09-15. Hash routes are not sitemap URLs.

## 5. Next repair

Bump displayed tags when a product repo ships. Keep
`index.html` constellation in lockstep with `llms.txt`.
